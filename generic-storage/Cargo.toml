[package]
name = "generic-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "1.0.228",features = ["derive"] }
serde_json = "1.0.149"
borsh = { version = "1.5", features = ["derive"] }
wincode = {version = "0.4.2",features = ["derive"]}
